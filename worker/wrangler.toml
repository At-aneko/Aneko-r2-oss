# Cloudflare Workers 名称
name = "aneko-r2-oss"
# 入口文件
main = "src/index.ts"
# 兼容日期
compatibility_date = "2024-12-01"

# R2 存储桶绑定
[[r2_buckets]]
binding = "R2_BUCKET"
bucket_name = "disk"  # 替换为你的 R2 桶名称

# 敏感信息（ACCESS_CODE / TURNSTILE_SECRET_KEY）使用面板 Variables 设置
# 部署后到 Worker 设置 → Variables → 添加密钥